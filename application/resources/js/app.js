import './bootstrap';
import '../css/app.css';

// Vue is imported selectively, not globally
// Import Vue components only where needed (wizards, single-page flows, interactive prototypes)

// Example: Uncomment to use Vue globally (not recommended for this architecture)
// import { createApp } from 'vue';
// 
// const app = createApp({});
// 
// // Register Vue components here if needed globally
// // app.component('ExampleComponent', require('./components/ExampleComponent.vue').default);
// 
// app.mount('#app');

